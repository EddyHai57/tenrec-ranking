# 2026-05-29

## 今日目标

- 本地实现 DIN 模型和 focused tests。
- 使用 `ctr-610578df3be5` 1M hist preprocessing run 跑 CPU overfit gate 和 100k/50k head smoke。
- 本地 commit / push 后，由服务器 read-only pull 并跑 DIN full 3 seeds。

## 已完成事项

- 新增 `Din` 模型，语义限定为 static user-level hist snapshot + target-dependent attention。
- DIN 使用同一份 `item_id` embedding 处理 target item 和 `hist_item`。
- Attention 输入为 `[target_emb, hist_emb, target_emb * hist_emb, target_emb - hist_emb]`，输出每个 hist 位置的标量权重。
- Attention 不做 softmax；仅对 padding index `1` mask 为 0，OOV index `0` 不 mask。
- 训练层接入 metadata 中的 `sequence_features`，使 CSV / tensor loader 的 `hist_item` 能传入 DIN。
- 新增 `configs/din_smoke.yaml`，指向 `outputs/preprocessed/ctr-610578df3be5/metadata.json`。
- 新增 `tests/test_din.py`，覆盖共享 embedding、padding/OOV mask、target-dependent attention、all-padding history、attention 非全等和 forward shape。
- 本地完成 commit / push，GitHub main 更新到 DIN 实现 commit `e52d5e5`。
- 服务器配置 read-only GitHub deploy key 后，`GitHub -> server` pull 链路恢复。
- 服务器 pull 后通过 `python -m unittest discover tests`，结果为 `Ran 28 tests OK`。
- 生成 full hist run 的 Linux 路径版 `metadata_server.json`，源数据为 `ctr-972e0dcb2b8d`。
- 在 `/root/autodl-tmp/server-configs-backup/` 生成 3 个 server-only DIN full config。
- 服务器 GPU overfit sanity 通过，seed `20260525` final loss 为 0.0。
- 按现有 hash-bucket 逻辑生成 `train_shuffled_seed20260525_b64.csv`，大小 7.3G。
- 通过 tmux session `din_multiseed` 完成 DIN full 3 seeds sweep，并生成 `outputs/runs/din_multiseed_summary.md`。

## 关键命令

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_din
.\.venv\Scripts\python.exe -m py_compile src\tenrec\models.py src\tenrec\training.py src\tenrec\torch_data.py scripts\train.py tests\test_din.py
.\.venv\Scripts\python.exe -m unittest tests.test_data_contract tests.test_metrics tests.test_torch_data tests.test_torch_models tests.test_din
.\.venv\Scripts\python.exe scripts\train.py --config configs\din_smoke.yaml --overfit --device cpu
.\.venv\Scripts\python.exe scripts\train.py --config configs\din_smoke.yaml --device cpu
```

服务器命令摘要：

```bash
git pull --ff-only origin main
PYTHONPATH=src /root/miniconda3/bin/python -m unittest discover tests
PYTHONPATH=src /root/miniconda3/bin/python scripts/train.py --config /root/autodl-tmp/server-configs-backup/server_din_full_s20260525.yaml --overfit --device cuda
tmux new -d -s din_multiseed /root/autodl-tmp/server-configs-backup/run_din_multiseed.sh
```

## 验证结果

DIN focused tests：

```text
Ran 6 tests OK
```

相关回归测试：

```text
Ran 23 tests OK
```

DIN overfit gate：

```text
initial_loss: 0.402283251285553
final_loss: 0.0
target_loss: 0.05
passed: true
```

DIN CPU smoke：

```text
run_id: 20260529-014749-din_smoke-din
metadata_run_id: ctr-610578df3be5
loader: tensor
best_epoch: 1
valid AUC: 0.6033750259652177
valid GAUC: 0.574797566693139
GAUC coverage: 0.89796
valid LogLoss: 0.5790715359423144
```

参数量：

```text
total_params: 3,768,850
embedding_params: 3,696,528
attention_params: 6,273
deep_params: 66,049
```

服务器 DIN full 3 seeds：

```text
run_ids:
- 20260529-012051-din_full_s20260525-din
- 20260529-013834-din_full_s42-din
- 20260529-015624-din_full_s2026-din

test AUC mean ± std: 0.7776414651 ± 0.0003614382
test GAUC mean ± std: 0.7192751046 ± 0.0001387128
coverage mean ± std: 0.8141143988 ± 0.0000000000
test LogLoss mean ± std: 0.4345373488 ± 0.0010328161
best_epoch: 1, 1, 1
```

## 输出文件

以下均在 ignored 目录，不进入 git：

```text
outputs/runs/20260529-014749-din_smoke-din/
outputs/runs/20260529-012051-din_full_s20260525-din/
outputs/runs/20260529-013834-din_full_s42-din/
outputs/runs/20260529-015624-din_full_s2026-din/
outputs/runs/din_multiseed_summary.md
```

## 当前限制

- 本次只使用 1M hist smoke run `ctr-610578df3be5` 的 head-truncated 100k train / 50k valid。
- 本地 CPU smoke 只证明 DIN 本地链路、mask、attention、loss/backward 和 smoke 训练可运行，不是正式指标。
- DIN full 结果来自 `ctr-972e0dcb2b8d` static hist snapshot，不能写成 DIEN 或动态兴趣演化。
- DIN full 未做超参调优，且只使用 5 个 ID/profile 特征加单条 `hist_item` 序列。
- 初始 loss 来自 train-base-rate output bias 和首个 batch label 分布；不能与未加 bias 的 `ln(2)` 直接等同。

## 下一步

- 记录 DIN multi-seed full 结果到 canonical docs。
- 经 Eddy 确认后 commit / push 文档更新。
- 进入 Phase C：dual-protocol 对照设计，拆分 strict mainline、hist ablation 和 official-compatible reproduction。

## Phase C 启动记录

已完成：

- 新增 PCOC calibration metric：`PCOC = mean(pred) / mean(label)`。
- 将 PCOC 接入训练评估输出，后续 `metrics.jsonl` / `summary.json` 会同时记录 AUC、GAUC、LogLoss、PCOC。
- 新增 official-compatible preprocessing 脚本：`scripts/preprocess_ctr_data_official.py`。
- official-compatible preprocessing 语义：复用 `ctr-972e0dcb2b8d` train-only vocab，执行 1:2 negative sampling、global hash-bucket shuffle、random 8:1:1 split，并输出独立 `ctr-*-official` run。
- 新增 focused tests：PCOC known-answer、official sampling / split / vocab reuse。
- 新增 smoke config：`configs/official_ctr_smoke.yaml`，用于 1M source-row official preprocessing smoke。

本地 smoke 结果：

- 重启 Codex 后普通 sandbox 仍报 `CreateProcessAsUserW failed: 1312`，但提权执行路径可用，已继续完成本地验证。
- PCOC / official preprocessing focused tests：`Ran 6 tests OK`。
- official 1M preprocessing smoke 生成 `ctr-e78027e6d936-official`，sampled rows 为 825,617，train base rate 为 0.33381428720667744。
- LR overfit gate 通过：initial loss 0.6931471824645996，final loss 0.012143656611442566。
- LR official 100k/50k smoke 跑通：valid AUC 0.7051061853624729，valid GAUC 0.6097681928970258，valid LogLoss 0.573972765834224，valid PCOC 1.0154340796581025。
- PCOC 语义需要澄清：当前定义相对采样后 label 分布计算，因此 official sampled valid 上 PCOC 接近 1；若要衡量相对原始 CTR 的校准失真，需要另加 original-distribution calibration 指标。

## Phase C full 服务器结果

已完成：

- 服务器 pull 到 `988528a` 后运行 `python -m unittest discover tests`，结果为 `Ran 31 tests OK`。
- Full official-compatible preprocessing 完成，run_id 为 `ctr-2ce095f2954f-official`。
- official full sampled rows 为 86,642,070；train / valid / test 为 69,313,656 / 8,664,207 / 8,664,207；train base rate 为 0.3333058784260348。
- vocab 与 `ctr-972e0dcb2b8d` 完全一致，official metadata 使用 server Linux 路径。
- 服务器完成 LR / DeepFM / DCN-v2 各 3 seeds official-compatible full training，并对每个 best checkpoint 在 strict test set 上做 cross-protocol evaluation。
- 汇总文件已生成：`outputs/runs/dual_protocol_summary.md`。

核心结果：

```text
Official->Official AUC mean:
LR      0.7866627243
DeepFM  0.7956704545
DCN-v2  0.7961370652

Official->Strict PCOC mean:
LR      1.4370512442
DeepFM  1.4507682883
DCN-v2  1.4562441234
```

结论：

- official-compatible 协议会把 AUC 抬高约 2.3pp，但这是采样分布上的分数。
- official-trained 模型在 official sampled test 上 PCOC 接近 1.0，在 strict test 上 PCOC 约 1.44-1.46，说明直接部署到真实分布会系统性高估 CTR。
- Phase C 支持继续把 strict protocol 作为主线，并把 official-compatible 结果作为复现 / 对照 / 反面校准案例。

已知限制：

- LogLoss 跨协议不可比。
- 当前是 official-compatible reproduction，不是 exact paper replication。
- 服务器 active repo outputs 下未找到 strict-trained baseline checkpoints，因此未做 Strict->Official 反向 cross-eval。
- `dual_protocol_summary.md` 最初未自动生成；手动重跑 server-only `summarize_phase_c.py` 后已生成成功。
