# 2026-05-24

## 今日目标

- 初始化 Tenrec 排序项目，但不提前创建训练代码。
- 复用 `D:\ANU\project\amazon-two-tower` 中有价值的 workflow 纪律。
- 在下载数据和 schema inspection 前，先定义文档和 agent 规则。

## 已完成事项

- 确认当前项目目录：`D:\ANU\project\tenrec-ranking`。
- 确认 scaffold 前目录没有初始项目文件。
- 检查 Tenrec official website、official GitHub repository、official dataset page 和 paper entry。
- 创建最小文档骨架。
- 轻量扫描 `D:\ANU\project\amazon-two-tower`，复用其项目规则和日志结构中适合本项目的部分。
- 将初始 Tenrec `AGENTS.md` 替换为排序项目专用规则文件。
- 新增 `docs/LOGGING_GUIDE.md`，参考 Amazon 的日志 workflow，但改成 Tenrec ranking 适用。
- 扩展 `.gitignore`，覆盖后续本地 logs、arrays、parquet files 和 checkpoints。
- 初始化本地 git 仓库。
- 添加 SSH remote：`git@github.com:EddyHai57/tenrec-ranking.git`。
- 验证 GitHub remote 可访问，且当前没有 refs。
- 扩展 `AGENTS.md` 的 Git / GitHub 规则，写入显式 SSH key 路径、push 命令、staged-file 检查、同步方向表达和本地 / 服务器边界。
- 将项目 Markdown 默认语言规范为简体中文，保留必要英文专有名称、命令、路径、模型名和指标缩写。

## 关键命令

```powershell
Get-Location
rg --files -g 'AGENTS.md' -g 'README.md' -g 'docs/**'
git status --short
git init
git branch -M main
git remote add origin git@github.com:EddyHai57/tenrec-ranking.git
git remote -v
git ls-remote origin
ssh -T git@github.com
Get-Content -LiteralPath 'D:\ANU\project\amazon-two-tower\AGENTS.md'
Get-ChildItem -LiteralPath 'D:\ANU\project\amazon-two-tower\docs'
Get-Content -LiteralPath 'D:\ANU\project\amazon-two-tower\docs\LOGGING_GUIDE.md'
Get-ChildItem -Recurse -File
Select-String -Path README.md,AGENTS.md,docs\*.md,docs\daily_logs\*.md -Pattern '<incomplete-marker-pattern>' -CaseSensitive:$false
git ls-files --others --exclude-standard
```

## 修改文件

```text
.gitignore
AGENTS.md
README.md
docs/LOGGING_GUIDE.md
docs/data_notes.md
docs/decision_log.md
docs/environment.md
docs/experiment_log.md
docs/issue_log.md
docs/project_summary.md
docs/daily_logs/2026-05-24.md
```

## 产出文件

- 仅创建文档 scaffold。
- 创建本地 `.git/` 仓库元数据。
- 未创建 `src/`、`configs/` 或 `data/` 目录。
- 未下载本地 Tenrec 数据。
- 未创建实验输出。

## 问题 / 风险

- 尚未创建首个 commit。
- 尚未 push 到 GitHub。
- Tenrec 本地 schema 仍未验证。
- 尚未创建 Python 环境。
- 后续服务器训练需要单独验证 GitHub access；不能假设 Windows SSH key 已存在于服务器。

## 当前 Git 状态

初始化前 `git status --short` 无法运行，因为目录还不是 git repo：

```text
fatal: not a git repository (or any of the parent directories): .git
```

初始化后 remote：

```text
origin git@github.com:EddyHai57/tenrec-ranking.git (fetch)
origin git@github.com:EddyHai57/tenrec-ranking.git (push)
```

`git ls-remote origin` exit code 为 0，且无 refs 输出，符合空 GitHub repository 状态。

## 今日结论

- 初始项目文档和 agent 规则已经就位。
- 仓库仍处于预 schema-inspection 阶段。
- 当前规则明确禁止过早创建 `src/` / `configs/`。
- 项目 Markdown 语言规则已固定为中文优先。

## 下一步最小动作

- 首个 commit 并 push 到 `origin/main`。
- 创建并激活 `.venv`。
- 获取最小官方 Tenrec 子集用于 schema inspection。
