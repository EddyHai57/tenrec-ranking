# 2026-05-25

## 今日目标

- 创建本地 `.venv`。
- 确认 Tenrec 是否存在官方最小可用子集。
- 对 `ctr_data_1M.csv` 做全量流式 schema inspection。
- 创建 Tenrec 数据总览文档并继续探查 `ctr_data_1M.csv` 数据契约风险。
- 生成用于 dataloader / split / GAUC 调试的 smoke samples。

## 已完成事项

- 确认创建前 `.venv` 不存在。
- 使用本机 Python `3.12.7` 创建 `.venv`。
- 将 `.venv` 内 `pip` 从 `24.2` 升级到 `26.1.1`。
- 未安装 PyTorch、pandas、pyarrow、scikit-learn 或训练依赖。
- 检查 Tenrec 官方 GitHub、官网、下载页和 arXiv 页面。
- 确认官方 GitHub 的 CTR benchmark 使用 `data/ctr_data_1M.csv`。
- 确认官网 leaderboard 标注 `CTR-1M`。
- 下载页静态 HTML / JS 未暴露直接 CSV 下载 URL。
- 发现用户已将 Tenrec 数据解压到 `data/Tenrec/`。
- 只读列出本地文件大小，没有解压、移动或删除数据。
- 读取 `Readme.txt` 和多个 CSV 头部。
- 使用 Python 标准库读取每个重点文件前 100,000 行做轻量抽样。
- 初步确认 `ctr_data_1M.csv` 包含 click 0/1 和 `hist_1` 到 `hist_10`，适合作为第一轮 CTR schema inspection 优先对象。
- 整合用户提供的 Tenrec 论文 / 官方材料截图：数据来源图、五个数据集特点、隐私匿名化说明和四场景统计表。
- 新增 `scripts/inspect_ctr_data.py`，使用 Python 标准库 `csv` 对 `ctr_data_1M.csv` 做全量流式 inspection。
- 先用临时小样本 unittest 验证统计逻辑，再运行真实数据。
- 全量读取 `data/Tenrec/ctr_data_1M.csv`，输出 JSON 和 Markdown 报告到 `outputs/inspection/`。
- 将全量 inspection 摘要写入 `docs/data_notes.md`。
- 新增 `docs/dataset_catalog.md`，按 raw scenario files 和 task-specific files 梳理 Tenrec 下载包。
- 新增 `scripts/probe_ctr_data_contract.py`，继续检查 user 顺序、重复、click 冲突、history 合理性和 split 策略。
- 全量运行 contract probe，输出 JSON 和 Markdown 报告到 `outputs/inspection/`。
- 将 contract probe 结论写入 `docs/data_notes.md` 和 `docs/project_summary.md`。
- 新增 `scripts/make_ctr_smoke_samples.py`，生成 head tiny sample 和完整 user block sample。
- 生成 `data/samples/ctr_tiny_100k_head.csv`。
- 生成 `data/samples/ctr_user_block_1m_seed20260525.csv`。
- 生成 `outputs/inspection/ctr_smoke_samples_summary.json` 和 `outputs/inspection/ctr_smoke_samples_report.md`。
- 将 smoke sample 口径、用途和限制写入 `docs/data_notes.md` 和 `docs/project_summary.md`。

## 关键命令

```powershell
if (Test-Path .venv) { '.venv exists' } else { '.venv absent' }
python --version
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install --upgrade pip
.\.venv\Scripts\python.exe --version
.\.venv\Scripts\python.exe -m pip --version
Invoke-WebRequest -Uri 'https://static.qblv.qq.com/qblv/h5/algo-frontend/tenrec_dataset.html' -UseBasicParsing
Invoke-WebRequest -Uri 'https://raw.githubusercontent.com/yuangh-x/2022-NIPS-Tenrec/main/README.md' -UseBasicParsing
Get-ChildItem -LiteralPath data -Recurse -Force -File
Get-Content -LiteralPath 'data\Tenrec\Readme.txt' -TotalCount 200
.\.venv\Scripts\python.exe - <rough-csv-head-and-sample-script>
.\.venv\Scripts\python.exe -m unittest tests.test_inspect_ctr_data
.\.venv\Scripts\python.exe scripts\inspect_ctr_data.py --input data\Tenrec\ctr_data_1M.csv --output-dir outputs\inspection
.\.venv\Scripts\python.exe -m py_compile scripts\probe_ctr_data_contract.py
.\.venv\Scripts\python.exe scripts\probe_ctr_data_contract.py --input data\Tenrec\ctr_data_1M.csv --output-dir outputs\inspection
.\.venv\Scripts\python.exe -m py_compile scripts\make_ctr_smoke_samples.py
.\.venv\Scripts\python.exe scripts\make_ctr_smoke_samples.py --input data\Tenrec\ctr_data_1M.csv --sample-dir data\samples --output-dir outputs\inspection --tiny-rows 100000 --target-rows 1000000 --seed 20260525
```

## 修改文件

```text
docs/environment.md
docs/data_notes.md
docs/project_summary.md
docs/daily_logs/2026-05-25.md
docs/dataset_catalog.md
scripts/inspect_ctr_data.py
scripts/probe_ctr_data_contract.py
scripts/make_ctr_smoke_samples.py
```

## 产出文件

- `.venv/` 本地虚拟环境，已被 `.gitignore` 排除。
- `data/Tenrec/` 本地数据目录，已被 `.gitignore` 排除。
- `outputs/inspection/ctr_data_1M_summary.json`，已被 `.gitignore` 排除。
- `outputs/inspection/ctr_data_1M_report.md`，已被 `.gitignore` 排除。
- `outputs/inspection/ctr_data_1M_contract_probe.json`，已被 `.gitignore` 排除。
- `outputs/inspection/ctr_data_1M_contract_probe.md`，已被 `.gitignore` 排除。
- `data/samples/ctr_tiny_100k_head.csv`，已被 `.gitignore` 排除。
- `data/samples/ctr_user_block_1m_seed20260525.csv`，已被 `.gitignore` 排除。
- `outputs/inspection/ctr_smoke_samples_summary.json`，已被 `.gitignore` 排除。
- `outputs/inspection/ctr_smoke_samples_report.md`，已被 `.gitignore` 排除。
- 文档更新。

## 全量 inspection 关键统计

`ctr_data_1M.csv`：

- size bytes：10,670,097,636。
- size GiB：9.9373。
- total rows：120,342,306。
- bad width rows：0。
- unique `user_id`：999,447。
- unique `item_id`：2,310,087。
- `click` 分布：`0`: 91,461,446；`1`: 28,880,860。
- `follow` 分布：`0`: 120,162,518；`1`: 179,788。
- `like` 分布：`0`: 118,066,889；`1`: 2,275,417。
- `share` 分布：`0`: 120,092,217；`1`: 250,089。
- `video_category` 分布：`0`: 64,475,979；`1`: 54,277,815；`\N`: 1,588,512。
- user row count summary：min 1，p50 82，p90 265，p99 590，max 5,995。
- item row count summary：min 1，p50 3，p90 51，p99 799，max 153,434。
- target `item_id` 出现在同一行 `hist_1` 到 `hist_10` 的次数：0。
- 无显式 timestamp 字段。

## 数据契约 probe 关键统计

`ctr_data_1M.csv`：

- user block count：999,447。
- user 连续排列：True。
- user_id 单调：True。
- non-contiguous user count：0。
- 完全重复行：883,646。
- 重复 `(user_id,item_id)`：1,810,484。
- 重复 pair 中 click 冲突：560,994。
- sampled user blocks：200。
- sampled history items：257,099。
- sampled history items not in previous rows of same file：257,099。

关键解释：

- `(user_id,item_id)` 不是唯一样本键，后续不能按 pair 直接去重。
- history item 大量不在同一文件此前行出现，说明 `ctr_data_1M.csv` 可能不是完整原始序列；不能据此证明有泄漏，也不能证明无泄漏。
- user-level order-based split 工程上可行，但 `hist_1` 到 `hist_10` 的构造口径仍需验证。

## Smoke sample 关键统计

`ctr_tiny_100k_head.csv`：

- rows：100,000。
- user count：773。
- file size bytes：7,767,741。
- `click=0`：71,756。
- `click=1`：28,244。
- header preserved：True。
- shuffled rows：False。
- deduplicated：False。

`ctr_user_block_1m_seed20260525.csv`：

- target rows：1,000,000。
- actual rows：1,000,200。
- user count：8,181。
- file size bytes：88,850,808。
- `click=0`：758,425。
- `click=1`：241,775。
- user row count summary：min 1，p50 84，p90 269，p99 607，max 1,810。
- complete user blocks：True。
- shuffled rows：False。
- deduplicated：False。

用途边界：

- tiny head sample 只用于快速 reader / parsing / batch shape debug。
- user-block sample 用于 dataloader、GAUC、user-level split、order-based split 和 tiny training / overfit smoke。
- 两者都不能写成正式实验结果。

## 问题 / 风险

- `ctr_data_1M.csv` 约 9.94 GB，不适合用 pandas 直接全量读入本地内存。
- 数据没有显式 timestamp；`Readme.txt` 说明时间信息隐含在用户级 item 顺序中。
- `hist_1` 到 `hist_10` 的构造是否严格只用过去行为仍未验证。
- 用户提供的论文 / 官方材料统计尚未由本地脚本重算，不能写成本项目实验结果。
- `video_category` 存在 `\N`，后续 dataloader 需要明确 missing category 编码。
- 存在重复行、重复 `(user_id,item_id)` 和 click 冲突；去重策略必须先定义数据契约，不能机械删除。
- smoke samples 只是开发样本，不是 full validation / full test。

## 当前 Git 状态

- `.venv/` 和 `data/` 不应进入 git。
- 文档更新尚未 commit。

## 今日结论

- 本地最小 Python 环境已创建。
- 官方材料确认存在 1M 级别 Tenrec 子集口径。
- 本地已存在 `ctr_data_1M.csv`，第一轮 schema inspection 优先对象确定为该文件。
- `ctr_data_1M.csv` 全量流式 inspection 已完成；该文件有 120,342,306 行，不是字面 1M 行。
- Tenrec 下载包总览已写入 `docs/dataset_catalog.md`。
- `ctr_data_1M.csv` 数据契约 probe 已完成；user block 连续排列，order-based split 工程上可行，但 history 泄漏仍未完全排除。
- smoke samples 已生成。下一步不是 full training，而是基于 sample 验证 dataloader、feature parsing、split 和 GAUC。

## 下一步最小动作

- 基于全量 inspection 结果，定义第一版数据契约。
- 决定在无显式 timestamp 的条件下，使用哪种 order-based / user-level chronological split 验证路径。
- 进一步验证 `hist_1` 到 `hist_10` 的构造口径，避免未来信息泄漏。
- 明确第一版 split / 去重 / history 字段使用口径。
- 基于 `ctr_tiny_100k_head.csv` 做 dataloader smoke。
- 基于 `ctr_user_block_1m_seed20260525.csv` 做 GAUC 和 user-level split smoke。
