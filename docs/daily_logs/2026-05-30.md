# 2026-05-30

## 完成事项

### Phase D 服务器端工作

1. **SSH 免密登录配置**：生成 ed25519 key，添加到服务器 authorized_keys，后续服务器操作全部自动化。

2. **代码同步**：服务器 `git pull --ff-only` 到 ca84c9f（含 DIN numeric_features 接入、ablation configs、PyTorch 环境说明）；服务器 unittest 37 tests OK。

3. **Phase D full preprocessing（with resource monitor）**：
   - source: `ctr-972e0dcb2b8d`，输出: `ctr-5580cbc9aa26-stats`
   - 耗时 5825 秒（≈ 97 分钟），peak RSS 6056.5 MiB
   - train 97,146,674 行 / valid 11,597,816 行 / test 11,597,816 行
   - 与 source 行数完全一致

4. **DIN overfit sanity**（Phase D stats metadata）：final_loss 0.0 < 0.05，PASS。

5. **Phase D ablation 训练（9 runs）**：
   - LR × 3 seeds（20260525 / 42 / 2026）
   - DCN-v2 × 3 seeds
   - DIN × 3 seeds
   - 全部完成，0 跳过，结果写入 `outputs/runs/phase_d_ablation_done.flag`

### 本地工作

6. **代码改动（已 commit ca84c9f）**：
   - `train.py` 加 `--seed` CLI override
   - `configs/phase_d_lr_full.yaml` / `configs/phase_d_dcnv2_full.yaml` 新增
   - `Din` 接入 numeric_features（models.py / training.py / tests/test_din.py）
   - AGENTS.md / CLAUDE.md 喵读取确认协议更新 + PyTorch 环境路径

7. **闸门验证**：
   - 闸门 1（数字一手抽验）：PASS，所有 baseline/stats/std/显著性差异 < 1e-4
   - 闸门 2（PCOC 定性）：FAIL-new-issue → 立 ISSUE-20260530-001

8. **ISSUE-20260530-001 立项**：OOF-vs-full-train 特征分布偏移，LR/DCN-v2/DIN test PCOC 8–11%，机制分析完成，修复方案列出，暂不阻塞收口 commit。

## 待提交（本 commit 提案范围）

- `docs/experiment_log.md`：Phase D ablation 章节
- `docs/issue_log.md`：ISSUE-20260530-001
- `docs/daily_logs/2026-05-30.md`（本文件）
- `STATE.md`：滚动更新至收口阶段
- `docs/project_summary.md`：添加 Phase D ablation 一行

## 已知限制

- peak RSS 6056 MiB，远超 1M 子集时外推的 < 1GB 预期；count tables 随全量数据线性增长，streaming 只避免了行数据存储，不避免字典增长。
- Phase B baseline 无 PCOC 数据，无法直接对照 PCOC 偏移方向。
